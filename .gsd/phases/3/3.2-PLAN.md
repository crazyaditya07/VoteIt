---
phase: 3
plan: 2
wave: 1
---

# Plan 3.2: Transaction Lifecycle Management

## Objective
Establish a complete transaction lifecycle state machine managing pending executions, error handling/revert parsing, and triggering automatic state refreshes upon confirmation.

## Context
- frontend/src/components/VotingInterface.tsx

## Tasks

<task type="auto">
  <name>Transaction Lifecycle State Machine</name>
  <files>
    - frontend/src/components/VotingInterface.tsx
  </files>
  <action>
    - Manage Wagmi's `useWriteContract` and `useWaitForTransactionReceipt` to build an absolute state map of the transaction.
    - Handle stages: "Ready to Vote", "Awaiting Wallet Confirmation", "Transaction Pending", "Vote Confirmed".
    - Automatically trigger the `refetchHasVoted()` and refetch counts immediately upon `isTxSuccess`.
    - Provide a robust transactional UX disabling interactions while `isPending` or `isTxConfirming` is true.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>User flow correctly locks the interface down during transaction processing and updates live the moment the chain confirms execution.</done>
</task>

<task type="auto">
  <name>Error Boundary and Revert Parsing</name>
  <files>
    - frontend/src/components/VotingInterface.tsx
  </files>
  <action>
    - Parse raw blockchain revert errors (e.g. "Already voted", "Invalid option index", or User Rejected Request).
    - Map parsed error strings into user-friendly textual messages displayed elegantly in the UI.
    - Ensure errors clear automatically or conditionally upon next interaction attempt.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>Interface catches Web3 exceptions, preventing react tree crashes, and elegantly notifying the user of the contract's explicit rejection reason.</done>
</task>

## Success Criteria
- [ ] Complex state transitions handled flawlessly.
- [ ] Meaningful error messages mapped from EVM reversion codes or rejected promises.
- [ ] Component perfectly resyncs state silently without a hard browser refresh after successful transactions.
