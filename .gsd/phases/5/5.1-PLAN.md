---
phase: 5
plan: 1
wave: 1
---

# Plan 5.1: Multi-Poll Smart Contract Refactor

## Objective
Refactor `Voting.sol` to act as a centralized registry managing thousands of distinct polls rather than a singular hardcoded proposal.

## Context
- contracts/Voting.sol
- test/Voting.ts
- scripts/deploy.ts

## Tasks

<task type="auto">
  <name>Contract Structuring for Multi-Poll Logic</name>
  <files>
    - contracts/Voting.sol
  </files>
  <action>
    - Establish a `uint256 public pollCount`.
    - Create a `Poll` struct: (`title`, `description`, `creator`, `createdAt`, `options` array, mapping of `hasVoted` per poll, `voteCounts`).
    - Expose `createPoll(string _title, string _description, string[] _options)`.
    - Update `vote()` function to accept `uint256 pollId` alongside `optionIndex`.
  </action>
  <verify>npx hardhat compile</verify>
  <done>Voting logic flawlessly encapsulates nested polling struct states.</done>
</task>

<task type="auto">
  <name>Contract Test & Deploy Suite Adjustments</name>
  <files>
    - test/Voting.ts
    - scripts/deploy.ts
  </files>
  <action>
    - Refactor Mocha tests to deploy a blank registry, create a poll dynamically, then verify double voting fails securely for that specific `pollId`.
    - Ensure assertions check that `hasVoted` maps efficiently (e.g. tracking `pollId => wallet => bool`).
  </action>
  <verify>npx hardhat test</verify>
  <done>All tests confirm isolation between distinctly created Poll IDs.</done>
</task>

## Success Criteria
- [ ] Refactored contract maintains 100% test coverage against new struct arrays.
- [ ] Contract successfully emits `PollCreated(pollId, creator)`.
- [ ] Contract securely handles multi-poll vote execution.
