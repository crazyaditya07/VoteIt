---
phase: 5
plan: 3
wave: 2
---

# Plan 5.3: Frontend React Router & Dashboard Expansion

## Objective
Implement dynamic routing within the React application, linking the secure Web2 backend logic dynamically with the localized Web3 polling mechanisms.

## Context
- frontend/src/App.tsx
- frontend/src/Router.tsx
- frontend/src/pages/Dashboard.tsx
- frontend/src/pages/VotePage.tsx

## Tasks

<task type="auto">
  <name>React Routing & Dashboard Creation</name>
  <files>
    - frontend/src/App.tsx
  </files>
  <action>
    - Install and configure `react-router-dom` v6+.
    - Establish explicit route borders: `/login`, `/register`, `/dashboard`, and `/vote/:pollId`.
    - Build a protected Dashboard boundary mapping the localized User object extracted securely via a React Auth Context interacting with the backend API.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>User navigating to dashboard without a JWT is redirected.</done>
</task>

<task type="auto">
  <name>Poll Creation Form & Dynamic Resolution Mapping</name>
  <files>
    - frontend/src/components/CreatePoll.tsx
    - frontend/src/pages/VotePage.tsx
  </files>
  <action>
    - Map a Form allowing the Dashboard user to fire `createPoll()` using Wagmi against the Refactored Contract.
    - Intercept `isTxSuccess` directly, capture the transaction receipt, extract the newly emitted `pollId`, and send a `POST /api/polls` saving ownership context mapping to the database instantly.
    - Update `VotingInterface` to accept a parameterized `pollId` from the URL, injecting dynamic arguments inside the existing `useReadContract` hooks automatically on load.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>Application acts natively full-stack. React connects to Mongo to view metadata, and Wagmi dynamically queries the multi-poll contract for isolated results.</done>
</task>

## Success Criteria
- [ ] Dashboard explicitly restricted securely.
- [ ] Creation flow triggers blockchain, parses event receipt, updates Backend smoothly.
- [ ] The core `VotingInterface` safely consumes variables from React Router natively mapping results.
