---
phase: 6
plan: 2
wave: 1
---

# Plan 6.2: Backend Route Implementation

## Objective
Enforce the strictly zero-trust relational boundry inside the API endpoint by resolving the RPC verifier actively rejecting false, untrusted frontend telemetry instantly.

## Tasks

<task type="auto">
  <name>Secure Poll Route Creation</name>
  <files>
    - backend/src/routes/pollRoutes.ts
    - backend/src/controllers/pollController.ts
  </files>
  <action>
    - Implement `POST /api/polls` protected dynamically by the active JWT.
    - Intercept `req.body` ensuring payload receives exactly: `{ txHash, contractAddress }`. (Frontend `pollId` drops; backend extracts it strictly via RPC).
    - Query `User.findById(req.user.id)` to extract reliable authenticated `walletAddress`.
    - Execute `verifyPollCreationTx` aggressively.
    - If successful, persist data rigidly into MongoDB leveraging `PollMeta.create()`.
    - Returns `201` successfully, `400` gracefully if parameters fail decoding or event missing.
  </action>
  <verify>Manual check to ensure JWT guards wrap logic prior to RPC extraction.</verify>
</task>

## Integration Test Checklist
- [ ] Prevents duplicated `txHashes`.
- [ ] Controller correctly proxies RPC extraction back over to MongoDB instances avoiding silent exceptions.
