---
phase: 6
plan: 1
wave: 1
---

# Plan 6.1: Backend Schema & Utility Expansion

## Objective
Establish the secure trust boundary schemas and server-side RPC utilities for verifying deployed Polls independently before assigning Mongo ownership.

## Tasks

<task type="auto">
  <name>Database Requirements</name>
  <files>
    - backend/src/models/User.ts
    - backend/src/models/PollMeta.ts
  </files>
  <action>
    - Update `User.ts` schema to include `walletAddress` (required for on-chain cross-referencing). Ensure it maps cleanly through registration or wallet linking routines.
    - Update `PollMeta.ts` schema to strictly enforce: `pollId` (unique), `contractAddress`, `creatorUserId` (ObjectId), `txHash` (unique string index), `createdAt` (timestamp).
  </action>
  <verify>Backend compiles successfully without typing issues against existing references.</verify>
</task>

<task type="auto">
  <name>RPC Verification Utility</name>
  <files>
    - backend/src/utils/ethereumRPC.ts
  </files>
  <action>
    - Install `ethers` on backend if missing.
    - Export `VotingABI` to the backend securely.
    - Write a utility function `verifyPollCreationTx(txHash, expectedContract, expectedCreator)`:
      * Fetches receipt from Sepolia RPC natively using `process.env.SEPOLIA_RPC_URL`.
      * Instantiates `ethers.Contract` or `ethers.Interface` to decode the transaction logs.
      * Find the `PollCreated(uint256,address)` event.
      * Verifies `creator == expectedCreator` explicitly.
      * Returns extracted `pollId` directly from the log data.
  </action>
  <verify>No direct execution yet; verified structurally against ethers API patterns.</verify>
</task>

## Integration Test Checklist
- [ ] Schema extensions compile safely.
- [ ] Ethers interface correctly maps `PollCreated` topics and arguments strictly.
