---
phase: 6
plan: 3
wave: 2
---

# Plan 6.3: Frontend Receipt Parsing & Secure POST

## Objective
Update the React frontend polling mechanism to capture full execution receipts securely, transmitting verification headers accurately toward the Web2 trust boundary.

## Tasks

<task type="auto">
  <name>Frontend Extractor Flow</name>
  <files>
    - frontend/src/components/CreatePoll.tsx
  </files>
  <action>
    - Adjust `useWaitForTransactionReceipt` logic intercepting `data` object locally upon confirmation specifically mapping `receipt.logs`.
    - Extract `pollId` accurately from the decoded `PollCreated` array index locally referencing `topics[1]` leveraging Wagmi `decodeEventLog()` dynamically.
    - Execute an `await fetch('/api/polls')` including:
      * Standard JSON string array containing `txHash` & `contractAddress`.
      * Map the active local JWT via authorization bearer headers strictly.
  </action>
  <verify>Ensure compilation structurally holds Web3/Web2 binding logic.</verify>
</task>

<task type="auto">
  <name>Frontend Error Handling Optimization</name>
  <files>
    - frontend/src/components/CreatePoll.tsx
  </files>
  <action>
    - Write structured responses capturing:
      * Reject/Timeout (Wagmi boundary intercept)
      * Network Mismatches (Sepolia block check wrapper)
      * Database Failures (RPC failure handling boundary 500 blocks natively surfacing via Toast/Error UI block).
  </action>
  <verify>React compilations check perfectly allowing graceful component transitions continuously mapping `isLoading` vs Database synchrony states.</verify>
</task>

## Integration Test Checklist
- [ ] No race conditions between DB creation and React state routing.
- [ ] Frontend never trusts raw components optimistically rendering data prior to absolute RPC verification limits.
