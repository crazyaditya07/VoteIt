---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Project Scaffold & Web3 Initialization

## Objective
Establish the frontend foundation using React + Vite, properly configured with TailwindCSS, wagmi, viem, and a base folder structure for the voting application.

## Context
- .gsd/SPEC.md
- .gsd/DECISIONS.md
- **Contract Address**: Deployed Sepolia contract from Phase 1.

## Tasks

<task type="auto">
  <name>Initialize React + Vite + Tailwind</name>
  <files>
    - package.json
    - vite.config.ts
    - tailwind.config.cjs
    - src/index.css
  </files>
  <action>
    - Initialize a new Vite project with the React + TypeScript template in a clean temporary directory or gracefully within the active root (respecting existing Hardhat config). Alternatively, use a clean `frontend` folder. *(Recommendation: generate inside `./frontend` directory to keep hardhat separate).*
    - Install `tailwindcss postcss autoprefixer` and initialize Tailwind.
    - Configure `tailwind.config.cjs` to scan `src/**/*.{js,ts,jsx,tsx}`.
    - Set up `src/index.css` with Tailwind directives (`@tailwind base`, etc.).
    - Maintain a modern, semantic HTML layout structure in `src/App.tsx`.
    - Note: Exclude advanced animations/transitions; keep styling clean, minimal, and functional.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>Vite builds the project successfully with TailwindCSS integrated and no compilation errors.</done>
</task>

<task type="auto">
  <name>Configure Wagmi + Viem + Wallet Connector</name>
  <files>
    - frontend/src/Web3Provider.tsx
    - frontend/package.json
  </files>
  <action>
    - Install `wagmi viem @tanstack/react-query`.
    - Create a `src/Web3Provider.tsx` context wrapper using Wagmi's `createConfig`.
    - Configure the network strictly to `sepolia`.
    - Setup `http` transport and MetaMask injected connector.
    - Wrap the application within `Web3Provider` inside `main.tsx`.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>The application builds cleanly with Web3 dependencies successfully installed and the provider correctly mounted in the React component tree.</done>
</task>

## Success Criteria
- [ ] Vite project inside `frontend` folder successfully compiles.
- [ ] Tailwind CSS utility classes render correctly.
- [ ] Wagmi config connects directly to the Sepolia Testnet.
